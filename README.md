# nebular

Nebular is a rust based tui browser that works with lua. 

The web has become very complicated over the last decade and it is impossible to remove a part once it has been added to the standard. 

nebular should seek to create a new standard that is way simpler and only works in the tui. 